<header>
  <nav>
      <a>TinyApp</a>
      <% if(loggedIn) { %>
      <div>
        <form action="/urls/new" method="post">
          <input type="text" name="longURL" placeholder="http://" style="width: 50vw;">
          <button type="submit">new URL</button>
        </form>
      </div>
      <% } %>
      <div class="user">
        <% if(loggedIn) { %>
        <a href="/logout">LOGOUT</a>
        <% } %>
      </div>
  </nav>
</header>